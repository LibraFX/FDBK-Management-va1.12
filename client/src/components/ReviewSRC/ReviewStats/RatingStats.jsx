import React from 'react';
import PropTypes from 'prop-types';
import $ from 'jquery';
import { StatStyles } from '../StyledComponents/StatsStyles';

const RatingStats = (props) => {
  // Styles
  const { StatContainers } = StatStyles;

  return (

  )
}